import styled from 'styled-components';

export const Container = styled.div`
  .checkmark {
    height: 20px;
    width: 20px;
  }

  li {
    margin-bottom: var(--m_mini);
  }

  label {
    margin-bottom: 0;
  }
`;
