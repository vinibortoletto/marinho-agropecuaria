import styled from 'styled-components';

export const Container = styled.div`
  button {
    font-size: 1rem;
    width: 100%;
    height: 3rem;
  }

  button#add_to_cart_and_go {
    background-color: var(--d_green);
    margin-top: 0.5rem;
    font-weight: normal;
    text-align: center;
  }
`;
