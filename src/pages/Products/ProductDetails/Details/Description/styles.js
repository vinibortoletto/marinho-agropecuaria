import styled from "styled-components";

export const Container = styled.div`
  margin-top: var(--m_md);
  text-align: justify;
  font-size: var(--fz_sm);

  h2 {
    font-weight: bold;
    margin-bottom: var(--m_mini);
  }
`;
