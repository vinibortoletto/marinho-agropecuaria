import styled from "styled-components";

export const Container = styled.section`
  background: var(--d_gold);
  padding: var(--m_sm);
  text-align: center;
  font-size: var(--fz_sm);

  span {
    font-weight: bold;
  }
`;
